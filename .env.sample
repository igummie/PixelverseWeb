# Local developer-tools PIN.
PIXELVERSE_DEV_PIN=change-me

# Optional: set this to keep signed developer-tool sessions valid across restarts.
# PIXELVERSE_DEV_SESSION_SECRET=replace-with-a-long-random-value
